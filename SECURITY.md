# SECURITY: stresscheck-r8

## 1. セキュリティ方針
本サービスの最大のセキュリティ方針は「個人情報を持たない」ことである。氏名・メールアドレス・社員番号・企業名・部署名を保存しないため、漏洩時の影響を最小化する。

## 2. 保存しない情報
- 氏名
- メールアドレス
- 社員番号
- 企業名
- 部署名
- 住所
- 電話番号
- 勤怠・給与・評価情報

## 3. 保存する情報
- ハッシュ化されたトークン
- トークン状態
- 回答JSON
- 計算済みスコア
- 高ストレス判定フラグ
- 面接指導希望フラグ

保存しない情報に group_code、部署コード、拠点コード、企業コードを含める。どの部署・拠点・企業に属する受検者かは外部サービス側で管理する。

## 4. トークン保護
- 128bit以上の乱数を利用する。
- 平文トークンはDB保存しない。
- DB保存時はHMAC付きSHA-256等でハッシュ化する。
- URLトークンはアクセスログに残る可能性があるため、アプリケーションログでは必ずマスクする。
- 結果ページに「URLを第三者に共有しない」注意文言を表示する。

## 5. ブルートフォース対策
- 存在しないトークンへのアクセスが一定回数を超えたIPを一時ブロックする。
- `/result/{token}` には `/survey/{token}` より厳しいレートリミットを適用する。
- レスポンスメッセージは token 存在有無を詳細に推測させないようにする。

## 6. CORS
- 受検者向けの public API は、ブラウザの設置場所を事前に特定できないため、Origin で認可せず、トークンで保護する。
- 管理画面や外部サービス連携など、呼び出し元を特定できる API は許可ドメインを管理画面で設定する。
- 本番で `*` を許可するのは受検者向けの public API に限定し、管理系/API 連携では禁止する。
- `localhost` は開発環境のみ許可する。

## 7. WordPress管理画面
- 専用 capability で制御し、外部には管理権限を渡さない。
- CSV出力は管理者のみ許可する。
- nonceを検証する（REST API は `wp_rest` nonce を使う）。
- 管理APIでは WordPress REST API の permission_callback を必ず実装する。

## 8. ログ
- 回答本文をログに出力しない。
- 平文トークンをログに出力しない。
- IP・User-Agentを保存する場合はハッシュ化する。
- 監査ログには管理者操作のみを記録する。

## 9. リスクと対策

| リスク | 対策 |
|---|---|
| 他人の結果閲覧 | 推測困難トークン、レートリミット、HTTPS |
| 個人情報漏洩 | 個人情報を保存しない |
| 不正サイトからの回答送信 | 推測困難トークン、トークン状態検証、レートリミット |
| 管理・外部連携APIの不正利用 | WP権限制御/APIキー、nonce、Origin allowlist、管理ログ |
| 回答改ざん | 送信後再回答不可、トークン状態管理 |
| トークン再利用 | submitted後のsubmit拒否 |


## 10. データ保持方針
- 回答生データ、個人結果、統計結果は初期実装では無期限保持とする。
- 将来、管理画面からバックアップ、アーカイブ、削除バッチを実行できるように拡張する余地を残す。
- 無期限保持であっても、氏名・メールアドレス・社員番号・企業名・部署名・group_code は保持しない。

## 11. 面接指導希望フラグ
- 受検者が結果画面で「面接指導を希望する」を押した場合、`interview_requested = 1` として保存する。
- これはトークンに紐づくセンシティブ情報であるため、管理画面・APIレスポンス・ログで過度に露出させない。
- 外部サービス側が誰のトークンかを管理しているため、実際の連絡・面接調整は外部サービス側で行う。
